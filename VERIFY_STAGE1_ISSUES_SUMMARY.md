# Stage 1 验证脚本问题总结

## 当前问题分析

### 1. Loss 差异巨大（约 40-50 倍）

**现象：**
- 模型 Loss: 0.003775
- 手动计算 Loss（三种方式）: 0.16-0.18
- 差异倍数: 约 40-50 倍

**可能原因：**
1. HuggingFace 的 MAE loss 计算使用了特殊的归一化或缩放（即使 `norm_pix_loss=False`）
2. Loss 计算方式与我们理解的不同（如只计算部分通道，或使用了不同的 patch 提取方式）
3. 需要使用与 HuggingFace 完全相同的 patch 提取方式（`patchify` 函数）
4. `logits` 可能已经包含了某种预处理（如归一化、缩放）

**影响：**
- 无法直接验证 Loss 计算的正确性
- 但可以继续使用最佳方式重建图像用于可视化
- 重点关注重建图像的可视化效果和 Masked PSNR（与训练目标更相关）

### 2. 相关性很低或为负

**现象：**
- Intensity 被掩码区域相关性: -0.0562
- DoLP 被掩码区域相关性: -0.0242
- sin(2*AoLP) 被掩码区域相关性: 0.2451
- cos(2*AoLP) 被掩码区域相关性: -0.1158

**可能原因：**
1. 重建质量差（模型训练不充分）
2. `unpatchify` 逻辑有问题（导致重建图像与原始图像不匹配）
3. `logits` 排列方式不正确

**影响：**
- 重建图像质量差
- 需要检查可视化图像，确认重建是否合理

### 3. 重建图像范围异常

**现象：**
- 重建图像范围: [-0.0903, 1.1281]
- 超出正常的 [0, 1] 范围

**可能原因：**
1. `unpatchify` 逻辑有误，导致值域错误
2. 模型预测值本身超出 [0, 1] 范围（训练数据可能未归一化）
3. 数据预处理不一致（训练和验证的数据范围不同）

**影响：**
- 需要 clip 到 [0, 1] 范围
- 可能影响 PSNR 计算的准确性

### 4. Masked MSE 与 Loss 差异大（约 15 倍）

**现象：**
- 模型 Loss: 0.003775
- Masked MSE: 0.057202
- 差异倍数: 约 15 倍

**可能原因：**
1. HuggingFace 的 loss 计算使用了特殊的归一化或缩放
2. Patch 级别 vs 像素级别的计算差异
3. 重建逻辑有问题（logits 排列或 unpatchify 错误）

**影响：**
- 无法直接验证 Loss 计算的正确性
- 但 Masked PSNR 仍然是有意义的指标（与训练目标相关）

## 修复措施

### 1. 添加了三种 logits 排列方式的验证
- 方式1：直接使用 logits（假设已是 Raster Order）
- 方式2：使用 ids_restore 重新排列
- 方式3：直接使用 logits 的原始顺序（不重新排列）
- 自动选择与模型 Loss 最接近的方式

### 2. 改进了 unpatchify 逻辑
- 使用 `permute` + `reshape` 代替 `einsum`
- 添加了更详细的诊断信息

### 3. 添加了重建图像范围检查
- 检查 clip 前后的差异
- 提供可能原因的诊断信息

### 4. 改进了 Loss vs Masked MSE 对比分析
- 添加了更详细的差异分析
- 提供了可能原因和建议

## 建议

### 短期建议
1. **重点关注 Masked PSNR**：虽然 Loss 差异大，但 Masked PSNR（12-13 dB）仍然是有意义的指标
2. **检查可视化图像**：确认重建是否合理
3. **如果重建图像看起来合理但 PSNR 低**：可能是模型训练不充分

### 长期建议
1. **深入研究 HuggingFace 的 MAE 实现**：了解 loss 计算的确切方式
2. **使用 HuggingFace 的 patchify/unpatchify 函数**：如果可用，直接使用模型内部的方法
3. **检查训练数据预处理**：确保训练和验证的数据范围一致

## 当前状态

- ✅ 代码可以运行，不会报错
- ✅ 可以生成可视化图像
- ✅ 可以计算 Masked PSNR（虽然与 Loss 差异大，但仍有参考价值）
- ⚠️ Loss 计算与模型 Loss 差异大（需要进一步研究）
- ⚠️ 重建图像相关性低（需要检查可视化图像）

## 下一步行动

1. 检查生成的可视化图像，确认重建是否合理
2. 如果重建图像看起来合理，说明 `unpatchify` 逻辑基本正确
3. 如果重建图像明显错误，需要进一步修复 `unpatchify` 逻辑
4. 重点关注 Masked PSNR（12-13 dB）和重建图像的可视化效果

