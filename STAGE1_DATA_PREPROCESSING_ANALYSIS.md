# Stage 1 数据预处理与归一化分析

## 数据流程检查结果

### ✅ **数值量级一致性：已确认**

**当前数据流程**：

1. **`process_polar_images` 函数输出**：
   ```
   形状: (H, W, 4)
   值范围: [0, 1]（所有通道统一）
   通道顺序: [Intensity, DoLP, sin(2*AoLP), cos(2*AoLP)]
   ```

2. **各通道的数值范围**：
   - **Intensity**: `S0 / 255.0` → `[0, 1]` ✅
   - **DoLP**: `sqrt(S1² + S2²) / S0` → `[0, 1]` ✅
   - **sin(2*AoLP)**: `(sin(2*AoLP) + 1) / 2` → `[-1, 1]` → `[0, 1]` ✅
   - **cos(2*AoLP)**: `(cos(2*AoLP) + 1) / 2` → `[-1, 1]` → `[0, 1]` ✅

3. **转换为 PIL Image**：
   ```python
   physics_img_uint8 = (physics_img * 255).astype(np.uint8)  # [0, 255]
   ```

4. **`ToTensor()` 转换**：
   ```python
   ToTensor()  # 自动将 [0, 255] uint8 → [0, 1] float32
   ```

5. **最终输入到模型**：
   ```
   形状: (B, 4, H, W)
   值范围: [0, 1]（所有通道统一）
   dtype: float32
   ```

### ✅ **结论：没有数值量级差异问题**

- ✅ 所有通道都在 `[0, 1]` 范围内
- ✅ 数值量级一致，没有通道间的量级差异
- ✅ 与预训练权重的期望范围一致（RGB 图像通常也是 `[0, 1]` 范围输入）

## 分布差异（正常现象）

虽然所有通道都在 `[0, 1]` 范围内，但它们的**分布特性**可能不同：

1. **Intensity（光强）**：
   - 分布取决于场景光照
   - 可能集中在某个范围（例如 0.3-0.7）
   - 这是正常的物理特性

2. **DoLP（偏振度）**：
   - 理论范围 `[0, 1]`
   - 通常集中在较低值（例如 0.05-0.3）
   - 这是正常的物理特性（大多数表面偏振度较低）

3. **sin(2*AoLP) 和 cos(2*AoLP)**：
   - 分布相对均匀（因为角度分布均匀）
   - 映射到 `[0, 1]` 后，0.5 对应角度为 0°

**这些分布差异是正常的**，反映了物理量的真实特性，不需要额外处理。

## 为什么不使用 ImageNet 归一化

### ImageNet 归一化的作用

ImageNet 归一化通常使用：
```python
mean = [0.485, 0.485, 0.485]
std = [0.229, 0.229, 0.229]
# 相当于将 [0, 1] 映射到大约 [-2.1, 2.1] 的范围
```

### 为什么不适合偏振数据

1. **统计特性不同**：
   - ImageNet 归一化假设数据分布接近 ImageNet 数据集
   - 偏振物理数据的分布完全不同

2. **破坏物理语义**：
   - DoLP 的绝对数值（0.05 vs 0.8）有明确的物理意义
   - 归一化会丢失这种绝对意义

3. **配合 `norm_pix_loss=False`**：
   - 我们已经设置 `norm_pix_loss=False`，让模型直接学习物理数值
   - 如果使用 ImageNet 归一化，会与这个策略冲突

## 数据验证（已添加）

代码中已添加数据范围验证：

```python
# [数据验证] 确保数据范围正确
if pixel_values.min() < 0 or pixel_values.max() > 1:
    print(f"⚠ 警告: pixel_values 超出 [0, 1] 范围")
    pixel_values = torch.clamp(pixel_values, 0.0, 1.0)
```

这确保了：
- 如果数据异常（超出 `[0, 1]` 范围），会被自动裁剪
- 防止异常值影响训练

## 与预训练权重的兼容性

### MAE 预训练权重期望

- **facebook/vit-mae-base** 的预训练权重是基于 ImageNet RGB 图像训练的
- 输入通常是 `[0, 1]` 范围的 RGB 图像（经过 `ToTensor()` 转换）
- 我们的数据也是 `[0, 1]` 范围，**数值范围兼容** ✅

### 通道数扩展策略

虽然预训练权重是 3 通道（RGB），我们通过以下策略扩展到 4 通道：

1. **前 3 个通道**：直接复制 RGB 权重
   - Intensity ← R（光强类似亮度）
   - DoLP ← G（偏振度类似绿色通道）
   - sin(2*AoLP) ← B（角度信息类似蓝色通道）

2. **第 4 个通道**：初始化为前 3 个通道的平均值
   - cos(2*AoLP) ← 前 3 个通道的平均值

这样既利用了预训练权重的特征提取能力，又保持了数值范围的一致性。

## 总结

### ✅ **当前方案是正确的**

1. **数值量级一致性**：✅ 所有通道都在 `[0, 1]` 范围内
2. **数据流程正确**：✅ `process_polar_images` → PIL Image → `ToTensor()` → `[0, 1]` float32
3. **与预训练权重兼容**：✅ 数值范围一致
4. **保留物理语义**：✅ 不使用 ImageNet 归一化，配合 `norm_pix_loss=False`
5. **数据验证**：✅ 已添加范围检查，防止异常值

### 📝 **建议**

当前的数据预处理方案是合理的，**无需修改**。如果训练时遇到问题，可以考虑：

1. **监控数据分布**：在训练开始时打印一些样本的统计信息（mean, std, min, max）
2. **检查异常值**：如果看到数据验证警告，需要检查 `process_polar_images` 函数
3. **调整学习率**：如果训练不稳定，可能需要调整学习率（因为输入分布与 ImageNet 不同）


