# Stage 1 MAE 训练问题诊断与建议

## 📊 当前状态

### 训练 Loss 趋势
- **Epoch 11.36**: Eval Loss: 0.012236
- **Epoch 454.55**: Eval Loss: 0.011728
- **下降幅度**: 仅 4.1%（几乎停滞）

### 验证结果（checkpoint-5250，约30%训练进度）
- **Masked PSNR**: 13.48 dB（较差，理想值应 > 20 dB）
- **模型 Loss**: 0.004479
- **Masked MSE**: 0.041763
- **差异倍数**: 9.32x（异常）
- **相关性**: -0.04 到 0.03（极低，说明重建失败）

## 🔍 问题分析

### 1. Loss vs Masked MSE 差异过大（9-10倍）

**可能原因**：
- MAE 的 `outputs.loss` 是在 **patch 级别**计算的（每个 patch 的像素值平均）
- 验证脚本计算的是 **像素级别**的 MSE
- 但即使考虑这个差异，9-10 倍的差异仍然异常

**需要检查**：
- HuggingFace 的 `ViTMAEForPreTraining` 的 loss 计算方式
- 是否 `norm_pix_loss=False` 时，loss 的计算方式与预期一致

### 2. 重建质量差

**症状**：
- Masked PSNR 仅 13.48 dB（理想值应 > 20 dB）
- 相关性接近 0 或为负
- sin/cos 通道 PSNR 很低（14.02 / 10.41 dB）

**可能原因**：
- 模型没有学到有效的重建
- 数据分布问题（DoLP 值太小，sin/cos 通道学习困难）
- 学习率太小，模型更新不足

### 3. 训练 Loss 几乎不下降

**症状**：
- Loss 从 0.012 降到 0.011，下降幅度极小
- 训练已进行 454 个 epoch，但 Loss 几乎停滞

**可能原因**：
- **学习率太小**（当前 1e-4，对于部分冻结的模型可能不够）
- 模型已收敛到局部最优
- 数据分布问题（DoLP 值太小，导致 Loss 本身就很低）

### 4. 重建图像值域异常

**症状**：
- 重建图像范围 [-0.04, 1.01]，超出 [0, 1]
- 说明模型预测可能不稳定

## 💡 建议方案

### 方案 A：调整学习率（推荐先试）

**修改 `train_stage1.py`**：
```python
learning_rate: float = 5e-4,  # 从 1e-4 提高到 5e-4
```

**理由**：
- 当前学习率 1e-4 可能太小，导致模型更新不足
- 对于部分冻结的模型，可以适当提高学习率
- 5e-4 是一个安全的起点

### 方案 B：检查数据预处理

**问题**：
- DoLP 通道均值仅 0.1022，值域很小
- sin/cos 通道在 [0, 1]，但 DoLP 通道值域不一致

**建议**：
- 检查数据预处理是否正确
- 考虑对 DoLP 通道进行归一化（但要注意保留物理意义）

### 方案 C：检查 Loss 计算

**问题**：
- Loss vs Masked MSE 差异 9-10 倍，异常

**建议**：
- 检查 HuggingFace 的 `ViTMAEForPreTraining` 的 loss 计算方式
- 确认 `norm_pix_loss=False` 时的行为是否符合预期
- 可能需要手动实现 loss 计算，确保与验证脚本一致

### 方案 D：重新训练（如果上述方案无效）

**如果调整学习率后仍然无效**：
1. 检查数据预处理是否正确
2. 检查模型配置（`norm_pix_loss`、`num_channels` 等）
3. 考虑使用更大的学习率（如 1e-3）
4. 考虑解冻更多层（当前只解冻最后 3 层）

## 🎯 立即行动

### 1. 先调整学习率（最简单）

修改 `train_stage1.py` 第 272 行：
```python
learning_rate: float = 5e-4,  # 从 1e-4 提高到 5e-4
```

然后重新训练，观察 Loss 是否下降更快。

### 2. 如果学习率调整后仍然无效

检查以下内容：
- 数据预处理是否正确（DoLP 通道的值域是否合理）
- MAE 的 loss 计算方式是否与预期一致
- 是否需要解冻更多层

### 3. 继续训练 vs 重新训练

**建议**：
- 如果 Loss 还在缓慢下降（即使很慢），可以继续训练
- 但如果 Loss 已经完全停滞（如最近 100 个 epoch 没有变化），建议重新训练

## 📝 结论

**当前训练状态**：❌ **不理想**

**主要问题**：
1. Loss 几乎不下降（4% 的下降幅度太小）
2. 重建质量差（PSNR 13.48 dB，相关性接近 0）
3. Loss vs Masked MSE 差异异常（9-10 倍）

**建议**：
1. **立即调整学习率**（从 1e-4 提高到 5e-4）
2. 如果调整后仍然无效，**检查数据预处理和 Loss 计算**
3. 如果问题持续，**考虑重新训练**（使用更大的学习率或解冻更多层）

**是否继续当前训练**：
- 如果 Loss 还在缓慢下降：✅ 可以继续，但建议先调整学习率
- 如果 Loss 已经完全停滞：❌ 建议停止并重新训练


